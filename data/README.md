MNIST dataset is downloaded automatically by `keras.datasets.mnist.load_data()`
when you run `src/train_cnn.py` or the notebook — no manual download needed
(requires internet on first run; Google Colab has this built in).

This folder is intentionally empty for that reason.
